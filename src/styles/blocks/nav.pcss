.nav {
  &__list {
    display: flex;
    align-items: center;
  }

  &__item {
    margin-right: 60px;

    &:last-child {
      margin-right: 0;
    }
  }

  &__link {
    text-decoration: none;
    white-space: nowrap;
  }

  &--color-white {
    color: #fff;
  }
}
