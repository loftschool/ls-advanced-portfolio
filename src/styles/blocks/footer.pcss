.footer {
    position: absolute;
    bottom: 0;
    background: $dark;
    width: 100%;

    &__container {
        display:flex;
        justify-content: space-between;
    }

    &__about {
        display: flex;
    }
}

.year {
    font-size: 48px;
    font-weight:bold;
    color: $light;
}
.my__footer {
    display:flex;
    flex-direction:column;
    justify-content: center;
    color: red;
}

